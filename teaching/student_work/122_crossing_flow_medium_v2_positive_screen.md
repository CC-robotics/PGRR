# 15米 crossing-flow / medium v2 单对正向筛选

## 修改边界

v2 只修正 v1 的任务距离，不改变交互机制或算法：

- 起点保持 `(5, 12)`；
- 终点由 `(26, 12)` 改为 `(20, 12)`，起终点距离正好 15 m；
- 最后一个行人交点仍为 `x=14`，其后保留 6 m 重新加入验证段；
- seed `95010`、两名行人、行人路线与速度范围、90 s 时限、PGRR checkpoint、
  恢复阈值均保持不变；
- 场景仍为 train-only，不生成或使用 held-out test。

场景 SHA-256 为
`5e230d5341a5e2ffbce025a0b970f1e23961b0f0749a7b98d0387431c3ee9e11`。
9/9 离线几何/语义门槛通过，4项单元测试和Ruff检查通过。

## 固定单对结果

| 方法 | 结局 | 样本数 | 物理终点距离 |
|---|---:|---:|---:|
| Base | `COLLISION`（robot-human overlap） | 271 | 8.648 m |
| PGRR | `GOAL_REACHED` | 757 | 0.206 m |

Base 在v1/v2中分别于272/271个样本碰撞，说明缩短终点没有破坏原本的行人冲突。
PGRR在25.841 s进入恢复，38.228 s进入REJOIN，38.728 s恢复原目标，最终约
75.7 s到达。

PGRR共记录11次决策：4次BACKUP、2次WAIT、3次CONTINUE，以及2次临时子目标
（`SUBGOAL_0`、`SUBGOAL_7`）。原始目标只记录为一个任务目标，并有1次明确恢复，
符合“临时恢复后重新加入原任务”的机制要求。

## 当前资格

该场景已经通过**单seed初筛**：同场景下Base动态碰撞而PGRR到达目标。它是目前
第一个满足目标方向的新增场景候选，但尚不能直接写成可复现论文结论：

- 单seed初筛候选：`1/8`；
- 完成重复验证的正式新增场景：`0/8`；
- 下一门槛：预先固定另外2个train seeds，要求至少2/3出现Base失败且PGRR到达；
- 之后才生成独立validation seeds，期间不再修改场景、阈值或模型。

完整运行材料位于
`outputs/student/advantage_scenario_screen_v2/runtime/`。
