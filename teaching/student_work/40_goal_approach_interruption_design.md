# 第 8 类场景：接近目标时的侧向中断

状态：参数与研究问题已固定为草案，尚未编译或启动 Arena。

机器人接近最终目标 3–5 m 时，行人从左右一侧横穿其局部路径；可选遮挡段长 1.80 m。该时段的局部规划通常已经朝目标收敛，突然横穿可能导致频繁换向、振荡或规划失败。

要检验的不是“绕过去就算成功”，而是 PGRR 是否只在持续可观测失败时介入，执行规划器可行的临时子目标后重新接回原始目标。首个 smoke 应使用 family #7 的 `low/train/r00`，按种子公式为 `91700`；正式比较仍须等待八类 protocol 和训练设置冻结。
