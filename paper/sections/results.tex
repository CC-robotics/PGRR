\ifmoderateresults
\subsection{Closed-Loop Navigation Outcomes}
Table~\ref{tab:moderate-main-results} reports all retained terminal outcomes for
the complete held-out three-density Gazebo benchmark.  Of \ModerateEpisodeCount{} logical
method--episodes, \ModerateValidEpisodeCount{} enter algorithm metrics and
\ModerateExcludedEpisodeCount{} are retained as simulator/reset exclusions.
Base DWB reaches \ModerateBaseSuccessRate{} of goals, collides in
\ModerateBaseCollisionRate{}, and times out in \ModerateBaseTimeoutRate{} of
its valid episodes.  PGRR reaches \ModeratePGRRSuccessRate{}, collides in
\ModeratePGRRCollisionRate{}, and times out in \ModeratePGRRTimeoutRate{}.

\input{generated/moderate_main_results.tex}

Figure~\ref{fig:moderate-outcomes-density} preserves each terminal category and
shows goal-reaching uncertainty by density.  Timeout and planner failure
remain explicit, so a lower contact rate cannot be misinterpreted as successful
recovery.  Exact family--density counts remain in the generated evaluation
artifacts and accompanying technical report.

\begin{figure*}[t]
  \centering
  \includegraphics[width=0.96\textwidth]{moderate_outcomes_and_density.pdf}
  \caption{Held-out three-density Gazebo outcomes.  (a) Stacked bars retain goal,
  collision, timeout, and planner-failure terminations.  (b) Goal-reaching
  rates by density include Wilson 95\% confidence intervals.  All methods use
  identical condition keys.}
  \label{fig:moderate-outcomes-density}
\end{figure*}

\subsection{Paired Effects Against Four Baselines}
On the \ModerateBasePGRRValidPairCount{} valid Base--PGRR pairs, the PGRR-minus-DWB
goal-reaching difference is \ModeratePGRRSuccessDifference{}
($p_{\mathrm{Holm}}\ModeratePGRRSuccessDifferenceHolmP$), the collision
difference is \ModeratePGRRCollisionDifference{}
($p_{\mathrm{Holm}}\ModeratePGRRCollisionDifferenceHolmP$), and the timeout
difference is \ModeratePGRRTimeoutDifference{}
($p_{\mathrm{Holm}}\ModeratePGRRTimeoutDifferenceHolmP$).  These endpoints are
interpreted jointly: goal reaching measures task completion, while collision
and timeout describe distinct failure modes.

Table~\ref{tab:moderate-pairwise-results} extends the same paired analysis to
Standard, Heuristic, and Uniform BC and reports raw and globally Holm-adjusted
tests, confidence intervals, and effect sizes without selecting a favorable
scenario or seed after evaluation.

\input{generated/moderate_pairwise_statistics.tex}

\subsection{Recovery Behavior}
Table~\ref{tab:moderate-recovery-results} reports triggers, successful
REJOIN-to-NORMAL transitions, time under recovery, intervention ratio, and
emergency-stop entries.  These quantities audit whether completion is obtained
through bounded recovery rather than persistent waiting; they do not replace
terminal navigation outcomes.

\input{generated/moderate_recovery_metrics.tex}

\else
\subsection{Artifact-Gated Outcome Status}
The complete moderate-v5 Base validation failed its frozen calibration ceiling
(57/72 goals, 79.17\%); consequently no v5 test episode was eligible for
analysis.  Moderate-v6 was then preregistered from validation evidence with the
single Crossing Flow timing change and new seed blocks described above.  This
source revision intentionally contains no v6 test number.  Final outcome,
paired-effect, and recovery tables appear only when the build finds a complete
v6 result manifest and the generated \texttt{moderate\_*} artifacts.
\fi

\subsection{Matched Execution Evidence}
Aggregate tables are complemented by one matched Base--PGRR condition selected
deterministically from the final \texttt{results.parquet}.  The release renderer
must join both method rows to their raw JSONL, metadata, and outcome sidecars,
verify the scenario and commit hashes, and print each terminal outcome verbatim.
This panel is illustrative ($n=1$) and is never used to estimate an effect.
The top-down trajectory and timeline are telemetry reconstructions, not camera
screenshots; Figure~\ref{fig:runtime-gazebo} separately provides an actual
Gazebo GUI pixel capture of the frozen environment.

\IfFileExists{figures/moderate_matched_base_pgrr_trajectory.pdf}{%
\IfFileExists{figures/moderate_pgrr_recovery_timeline.pdf}{%
\begin{figure*}[t]
  \centering
  \includegraphics[width=0.57\textwidth]{moderate_matched_base_pgrr_trajectory.pdf}\hfill
  \includegraphics[width=0.39\textwidth]{moderate_pgrr_recovery_timeline.pdf}
  \caption{Matched measured execution generated from final Parquet and raw
  episode streams.  Left: Base and PGRR trajectories for the same condition,
  with the recorded terminal outcome printed for each method.  Right: PGRR
  failure score, goal distance, recovery state, and action over simulation
  time.  These are telemetry reconstructions, not simulator or onboard camera
  screenshots; the actual Gazebo scene is shown separately in
  Figure~\ref{fig:runtime-gazebo}.}
  \label{fig:matched-runtime}
\end{figure*}
}{}% timeline
}{}% trajectory

\subsection{Offline Imitation and Mask Diagnostics}
The separate offline ablation in Table~\ref{tab:offline-ablation} compares
Uniform BC, margin-weighted BC, and the selected DAgger checkpoint on the same
scenario-disjoint validation states.  Margin weighting is retained as a
negative result rather than a selected contribution.  The no-mask rows are
counterfactual mask-dependence diagnostics and are not executed in simulation.
They test expert agreement and invalid-action exposure, not a controlled
closed-loop causal effect; the complete five-method closed-loop
evaluation above remains the evidence for navigation performance.

\input{generated/offline_ablation.tex}
