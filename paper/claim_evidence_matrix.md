# Claim–evidence matrix

All pre-verified-pose dynamic comparisons are diagnostic-only after KI-057--KI-060. They remain in the repository to preserve failure provenance but cannot support manuscript performance claims.

| Draft claim | Evidence | Artifact | Statistical status | Counterexample / limit |
|---|---|---|---|---|
| Gazebo-reported robot and pedestrian poses agree with sensed geometry in the selected pair. | Base 26/26 and DAgger 30/30 near-human frames pass. | `data/manifests/crossing_flow_medium_validation_s02201_2164e08_{base,bc}_r1_lidar_consistency.json` | Simulator-validity check | A run without a near encounter is insufficient evidence. |
| The selected checkpoint never emits a masked action in offline validation. | Invalid action rate 0.0. | `checkpoints/dagger/coverage_safety_aligned/metrics.json` | Deterministic mask property | Requires at least one valid fallback. |
| The current checkpoint can recover one verified-pose crossing-flow Base failure. | DWB collides at 29.47 s; DAgger physically reaches the goal at 98.43 s with 0.299 m physical goal error. | `outputs/pilot/crossing_flow_medium_s02201_2164e08_pair.csv` plus raw SHA256 values | Single paired validation episode; no significance claim | One seed cannot establish robustness or statistical superiority. |
| The current checkpoint recovers the observed current-version high-density failure while preserving two nominal successes. | Across seeds 2201, 2202, and 2220, DWB reaches 2/3 goals and collides once; triggered DAgger physically reaches 3/3 goals. All six LiDAR gates pass and terminal physical distances are explicit. | `outputs/pilot/crossing_flow_high_{s02201,s02202,s02220}_8577ff1_pair.csv` and matching `data/manifests/*8577ff1*lidar_consistency.json` files | Three paired validation seeds; descriptive pilot only | One scenario family and $n=3$ do not support significance or generalization; median terminal time increases from 91.7 to 128.4 s. |
| The selected checkpoint does not yet preserve every Base-solvable validation episode. | On pre-fix seed 2220, Base reaches the goal in 92.91 s while DAgger times out with 1401 recovery samples. | `outputs/pilot/crossing_flow_high_s02220_b2a9ac4_pair.csv` plus raw SHA256 values | Retained counterexample; no exclusion | The final 60 s expose a fallback pedestrian-yield live-lock. All methods must be replayed after the environment fix; this row prevents a nominal non-degradation claim. |
| Allowing receding fallback pedestrians removes the seed-2220 live-lock. | Base reaches the goal; DAgger changes from timeout to a success callback with 476 recovery samples. | `outputs/pilot/crossing_flow_high_s02220_30a1337_pair.csv` plus raw SHA256 values | Regression diagnostic only | The legacy outcome lacks the asynchronous terminal pose snapshot and the final periodic row is 0.319 m from goal; fresh replay is required for accepted goal evidence. |
| The current checkpoint does not generalize to the retained temporary-blockage validation case. | Base collides at 38.00 s; DAgger collides at 41.03 s after 71 recovery samples. | `outputs/pilot/temporary_blockage_high_s02720_9c1ed29_pair.csv` and matching LiDAR manifests | Single cross-family validation counterexample | The observable detector misses off-axis radial closing until emergency intervention is too late; no cross-family success claim is authorized. |
| Off-axis trend detection alone does not repair temporary blockage. | Under corrected commit `461ddc6`, Base collides at 38.00 s and DAgger at 40.49 s with 55 recovery samples. | `outputs/pilot/temporary_blockage_high_s02720_461ddc6_pair.csv` and matching LiDAR manifests | Retained validation regression failure | Online callback/nearest-return identity prevents the reconstructed early trend from becoming reliable control lead time. |
| The map-conditioned open-map guard does not apply to mapped doorway blockage, and the retained executor still fails there. | Under `e923536`, Base collides at 38.00 s and DAgger at 39.99 s with 65 recovery samples; both sensor gates pass. The single BACKUP raises clearance by 0.069 m before that gain is lost during deceleration. | `outputs/pilot/temporary_blockage_high_s02720_e923536_pair.csv` and matching LiDAR manifests | Retained validation regression failure | The scenario contains 20 static doorway objects, so enabling an open-map absolute threshold would be methodologically invalid; the next correction targets bounded escape state instead. |
| The current full hierarchy can recover the retained temporary-blockage Base failure. | Under `0205d6e`, Base collides at 37.995 s; recovery physically reaches the goal at 80.919 s with 0.195 m terminal error and 0.781 m minimum human distance. Both LiDAR gates pass at 100%. | `outputs/pilot/temporary_blockage_high_s02720_0205d6e_pair.csv` and matching LiDAR manifests | Single cross-family validation pair; no significance claim | Command traces show deterministic emergency turn/forward escape, not a learned temporary subgoal. This supports the hierarchy but does not isolate DAgger's contribution; exact emergency-mode logging is required in future runs. |
| The current hierarchy executes learned temporary subgoals and recovers one held-out group-blocking Base failure. | Under `dcd9bfe`, Base collides at 42.990 s; recovery physically reaches the goal at 112.887 s with 0.197 m terminal error and 0.873 m minimum human distance. Learned action IDs 2, 3, and 11 occur, and both LiDAR gates pass at 100%. | `outputs/pilot/group_blocking_high_s02420_dcd9bfe_pair.csv` and matching LiDAR manifests | Single cross-family validation pair; no significance claim | The hierarchy also uses deterministic safety modes and takes 69.9 s longer than the Base collision time. One seed cannot establish cross-family generalization or isolate the learned component. |
| Triggered DAgger is more efficient than the heuristic on one strict same-commit group-blocking check. | Base and Standard collide; Heuristic and DAgger reach the goal. DAgger takes 128.94 versus 152.38 s, uses 395 versus 606 recovery samples, and maintains 1.019 versus 0.841 m minimum human distance. | `outputs/pilot/group_blocking_high_s02420_926cc95_methods.csv` and four matching LiDAR manifests | Single validation seed; descriptive mechanism check only | Both learned and deterministic safety components are active. The observed 23.44 s difference is not a statistical ablation and may vary with Gazebo scheduling. |
| The frozen hierarchy recovers one high-density overtaking collision under the current verified-pose implementation. | DWB collides after 82.02 s at 0.685 m human-centre distance; triggered DAgger physically reaches the goal after 178.39 s with 0.224 m terminal error and 1.156 m minimum human distance. Both LiDAR gates pass every near-human frame. | `outputs/pilot/overtaking_high_s02520_324fcdf_pair.csv` and matching `data/manifests/*324fcdf*lidar_consistency.json` files | Single same-commit validation pair; descriptive only | Completion has only 1.61 s timeout margin and uses 1,133 recovery samples, so this supports recovery capability but also severe conservatism. |
| The frozen hierarchy avoids one blind-corner static collision but does not complete the task. | DWB physically intersects declared static geometry after 49.48 s; triggered DAgger remains collision-free for 180 s and makes 13.47 m progress but times out 8.16 m from the physical goal. | `outputs/pilot/blind_corner_high_s02320_fafcddd_pair.csv` with embedded raw hashes | Single same-commit validation pair; retained counterexample | Neither episode enters the 1.3 m near-human audit radius, so the pedestrian LiDAR gate is explicitly inconclusive. Avoiding collision is not recovery success. |
| The radial no-braking static refinement is unsafe and rejected. | Original and first candidate time out 5.088 m and 1.007 m from the train goal; the refined candidate reaches that train goal, but a cross-scenario replay physically hits a temporary-blockage door frame at 72.494 s during emergency rotation. | `outputs/pilot/blind_corner_train_static_margin_iteration.csv` and `outputs/pilot/temporary_blockage_high_s02720_a26e3b6_pair.csv`, both with raw hashes | Train improvement plus retained cross-scenario safety counterexample | A successful train episode cannot justify retaining a candidate that creates a new physical static collision; the no-braking refinement is removed. |
| Static-aware margins improve but do not solve held-out blind corner. | Same-commit DWB collides after 45.987 s; the hierarchy remains collision-free for 180 s but times out 5.385 m from goal, compared with 8.162 m for the retained pre-fix hierarchy. The learned sensor gate passes 196/196 near-human frames. | `outputs/pilot/blind_corner_high_s02320_8c92b00_pair.csv` and matching consistency manifests | Single held-out validation pair plus a historical pre-fix comparison; descriptive only | The corrected hierarchy does not reach the goal. Cross-revision distance reduction is diagnostic, not a paired causal estimate, and no further validation tuning is permitted. |
| The selected checkpoint does not recover the untouched high-density opposite-stream case. | Base collides at 3.497 s; recovery times out at 180 s with 0.200 m net progress and 1,633 recovery samples. Both LiDAR gates pass at 100%. | `outputs/pilot/opposite_streams_high_s02620_5a8de7e_pair.csv` and matching LiDAR manifests | Retained single-seed cross-family counterexample | Collision is avoided, but the final 60 s have negative progress. This is safe stagnation and requires train-split coverage expansion, not a positive recovery claim. |
| Two train-split corrections do not solve opposite-stream safe stagnation. | Five-second turn commitment removes turn-side switches but changes progress from +2.378 m to -0.146 m; sequence-aware Iteration 5 increases BACKUP/subgoal execution yet progresses only 0.196 m. Every run times out and both candidates are rejected. | `outputs/pilot/opposite_streams_train_ablation.csv`, Table V, and Fig. 5; source-row SHA256 values are embedded in the CSV | Three separately versioned diagnostic train runs; descriptive failure analysis, not a paired comparison | The interventions alter action occupancy but do not establish causal effects under identical code or improve task completion. |
| DAgger covers policy-induced recovery states. | Two aggregation rounds completed only on train split. | `data/manifests/dagger_iter1_manifest.json`, `data/manifests/dagger_iter2_manifest.json` | Pipeline provenance | DAgger-2 was worse in pilot and is a negative ablation. |
| The safety layer no longer traps the learned policy in the retained medium-density rear-obstacle counterexample. | Old policy timeout with 1275 recovery samples; corrected policy goal reach with 219. | `outputs/pilot/crossing_flow_medium_validation_escape_regression.csv` plus raw SHA256 values | Exact-scenario regression only | Base is faster on this scenario; no superiority claim. |
| Failed-rejoin retries no longer repeat CONTINUE when a planned escape exists. | Retained learned `PLANNER_FAILURE`; corrected fresh replay goal reach with 187 recovery samples. | `outputs/pilot/crossing_flow_high_validation_rejoin_regression.csv` plus raw SHA256 values | Regression evidence only | Same-seed Gazebo scheduling differs between runs. |
| Collision-specific margins persist through emergency release and multi-obstacle escape. | Retained timeout and two collision counterexamples followed by a goal reach with 1.161 m minimum human distance. | `outputs/pilot/crossing_flow_high_validation_safety_iteration.csv` plus raw SHA256 values | Development regression chain | Final configuration still needs repeated locked-code evaluation. |
| Scenario-aware contact classification does not terminate the retained open-map LiDAR self-return counterexample. | Old classifier reported collision; corrected fresh replay passed that point and reached the goal with 1.185 m minimum human distance. | `outputs/pilot/crossing_flow_high_validation_static_collision_classifier_regression.csv` plus raw SHA256 values | Classifier regression only | Static-geometry scenarios retain the LiDAR classifier; same-seed Gazebo timing differs. |
| Proxy-synchronized, self-return-filtered recovery completes the retained high-density development scenario without human overlap. | Final replay reached the goal in 176.923 s with 0.995 m minimum human distance. | `outputs/pilot/crossing_flow_high_validation_runtime_alignment_iteration.csv` plus raw SHA256 values | Development regression only | Close to timeout; preceding variants include collisions/timeouts; no multi-scenario claim. |
| On an independent medium-density validation scenario, the frozen candidate trades collision for safe stagnation. | Base collided at 29.204 s; recovery timed out without collision after 9.210 m progress. | `outputs/pilot/crossing_flow_medium_validation_candidate_100b09f_pair.csv` plus raw SHA256 values | Single paired scenario; no significance claim | Learned method did not reach the goal and used 1286 recovery samples. |

Rows referring to pre-verified-pose online runs are regression history only, irrespective of their earlier wording. No final multi-scenario performance claim is authorized until corrected training and locked test artifacts exist.
